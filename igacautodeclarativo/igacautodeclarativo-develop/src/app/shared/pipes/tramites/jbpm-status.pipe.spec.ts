import { JbpmStatusPipe } from './jbpm-status.pipe';

describe('JbpmStatusPipe', () => {
  it('create an instance', () => {
    const pipe = new JbpmStatusPipe();
    expect(pipe).toBeTruthy();
  });
});
